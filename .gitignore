# Inner repos: managed as plain clones via git_repositories.toml, not tracked.
/pipmake/
/ksgpu/
/pirate/

# Per-workspace, machine-specific, generated files.
.venv/
.direnv/
.envrc
.claude/env.sh
.claude/settings.json
.claude/settings.local.json
.agent/

# Ephemeral.
# No trailing slash on 'research': it is normally a symlink onto a scratch
# filesystem, and a trailing-slash pattern matches only real directories.
plans/
research
scratch*.txt
__pycache__/
*.pyc
*~